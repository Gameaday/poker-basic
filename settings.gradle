pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

// Pokermon - Pure Kotlin-Native Cross-Platform Project
rootProject.name = "Pokermon"

// Include Android module
include ':android'

// Include shared Kotlin-native module (source from Poker-Basic)
include ':shared'
project(':shared').projectDir = new File('Poker-Basic')

// Include desktop module for native executable generation  
include ':desktop'